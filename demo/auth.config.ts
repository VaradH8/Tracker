/**
 * Legacy NextAuth config — superseded by the custom cookie + DB session
 * auth in `lib/auth.ts`. Kept as an empty stub so anything that still
 * imports from here doesn't crash; safe to delete in a follow-up.
 */
export {};
